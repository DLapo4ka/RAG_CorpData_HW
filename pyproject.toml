[project]
name = "rag-homework"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "scikit-learn",
    "pytest",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.uv]
package = true
